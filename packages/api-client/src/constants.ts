/**
 * The base path where the Vite web app is served.
 * The shell owns the origin root; this constant propagates that base to
 * all per-tool SEO page CTAs in the (currently unrouted) content app.
 */
export const WEB_APP_BASE_URL = '/';
