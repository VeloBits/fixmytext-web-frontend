/**
 * The base path where the Vite web app is served.
 * The editor runs under the /app/* route prefix; this constant propagates
 * that base to all per-tool SEO page CTAs in the content app.
 */
export const WEB_APP_BASE_URL = '/app';
