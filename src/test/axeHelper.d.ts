/** Type declaration for axeHelper.js — kept as .js per project convention. */
export declare function expectNoA11yViolations(
  container: Element,
  ruleOverrides?: Record<string, { enabled: boolean }>,
): Promise<void>;
